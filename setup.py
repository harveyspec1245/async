from setuptools import setup

setup(
   name='asyncio',
   version='1.0',
   description='A useful module',
   author='Man Foo',
   author_email='foomail@foo.com',
   packages=['asyncio'],
   install_requires=[],
)
